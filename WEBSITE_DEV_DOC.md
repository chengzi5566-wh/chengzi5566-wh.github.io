# 个人产品推广网站 - 开发文档

> **版本**：v1.0
> **创建日期**：2026-09-03
> **托管平台**：GitHub Pages（免费静态网站托管）
> **架构模式**：数据驱动 + 纯静态渲染

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术选型与方案对比](#2-技术选型与方案对比)
3. [整体架构设计](#3-整体架构设计)
4. [页面结构设计](#4-页面结构设计)
5. [数据结构设计（核心 Schema）](#5-数据结构设计核心-schema)
6. [目录结构规划](#6-目录结构规划)
7. [扩展性设计（重点）](#7-扩展性设计重点)
8. [UI/UX 设计规范](#8-uiux-设计规范)
9. [GitHub Pages 部署方案](#9-github-pages-部署方案)
10. [开发计划与里程碑](#10-开发计划与里程碑)
11. [后续扩展方向](#11-后续扩展方向)

---

## 1. 项目概述

### 1.1 网站定位

本网站是开发者个人产品的**官方展示门户**，用于：

- 对外展示已开发的软件系统（当前 2 套，未来可扩展）
- 介绍每套系统的核心功能、技术亮点、适用场景
- 发布版本更新日志与迭代历程
- 提供文档下载与使用指引入口
- 建立专业的个人技术品牌形象

### 1.2 目标受众

| 受众群体 | 关注点 |
|---------|--------|
| 潜在用户 | 系统能解决什么问题、是否易用、如何获取 |
| 学校/机构采购方 | 系统稳定性、技术栈、数据安全、部署方式 |
| 技术同行 | 技术选型、架构设计、开源可能性 |
| 开发者本人 | 持续维护成本、内容扩展是否便捷 |

### 1.3 当前系统矩阵

| 系统编号 | 系统名称 | 定位 | 技术栈 |
|---------|---------|------|--------|
| S01 | 教师智能办公平台 | AI 赋能中小学教师全流程教学办公 | Python FastAPI + 原生前端 + Tailwind CSS |
| S02 | 智能办公协作平台 | 面向管理人员的文件收集与团队协作中枢 | Flask + Vue 3 + Element Plus |

> 两套系统形成「教学 + 管理」互补关系，未来可继续追加 S03、S04……

### 1.4 核心设计原则

1. **数据驱动**：所有内容从数据文件读取，HTML 不写死业务内容
2. **零构建**：纯静态文件，无需打包工具，推送到 GitHub 即可上线
3. **高内聚低耦合**：数据层、渲染层、样式层分离，改内容不动代码
4. **可扩展**：新增系统只需加一条 JSON 记录，无需修改任何 JS 逻辑
5. **响应式**：桌面/平板/手机自适应
6. **SEO 友好**：语义化 HTML、动态 meta 标签、sitemap 支持

---

## 2. 技术选型与方案对比

### 2.1 方案对比

| 维度 | 方案A：纯静态数据驱动（推荐） | 方案B：静态站点生成器（VitePress/Hugo） | 方案C：React/Vue SPA |
|------|---------------------------|--------------------------------------|-------------------|
| 构建步骤 | 无，编辑即生效 | 需要 npm install + build | 需要 npm install + build |
| GitHub Pages 支持 | 原生支持，直接推送 | 需配置 GitHub Actions 自动构建 | 需配置构建流程 |
| 维护成本 | 极低（改 JSON） | 中等（改 Markdown + 配置） | 较高（需懂框架） |
| 扩展性 | 高（Schema 驱动） | 高（主题 + 数据） | 高 |
| 首次加载速度 | 极快 | 快 | 较慢（需加载框架） |
| SEO | 好（服务端渲染 HTML） | 极好 | 一般（需 SSR 方案） |
| 适合人群 | 个人开发者，追求简单 | 技术博客/文档站 | 复杂交互应用 |

### 2.2 推荐方案：纯静态数据驱动

**技术栈**：

- **HTML5**：语义化标签构建页面骨架
- **Tailwind CSS（CDN）**：原子化样式，快速构建现代界面
- **Vanilla JavaScript（ES6+）**：原生 JS 读取 JSON 数据并动态渲染
- **Font Awesome（CDN）**：图标库
- **Google Fonts / 思源黑体**：中文字体优化

**为什么选这个方案**：

1. 你的核心诉求是「不要写死，后面还能增加内容和系统」——数据驱动完美满足
2. 个人维护，越简单越好——零构建，改完 JSON 直接 git push 就上线
3. GitHub Pages 原生支持——推送即用，不需要任何 CI/CD 配置
4. 加载速度快，SEO 好——对产品展示站很重要

**已知取舍（Tailwind CDN）**：

- Tailwind CDN 版本会加载约 300KB 的未压缩 CSS，对首次加载速度有一定影响
- 但对于产品展示站（页面内容不多、交互简单），这个影响可以接受
- 若未来对加载速度有更高要求，可切换到 Tailwind CLI 构建（需引入 npm 构建步骤），数据层和渲染层代码无需改动
- 此方案优先保证「零构建 + 易维护」，加载速度是可接受的取舍

---

## 3. 整体架构设计

### 3.1 架构分层

```
┌─────────────────────────────────────────────────┐
│                  浏览器层                        │
│   用户访问 → 加载 HTML → 加载 CSS/JS → 动态渲染   │
└────────────────────┬────────────────────────────┘
                     │ fetch()
┌────────────────────▼────────────────────────────┐
│                  数据层                          │
│   data/                                          │
│   ├── systems.json      （系统列表与详情）        │
│   ├── features.json     （功能模块定义）          │
│   ├── changelogs.json   （版本更新日志）          │
│   ├── testimonials.json （用户评价/案例，可选）    │
│   └── site.json         （站点全局配置）          │
└────────────────────┬────────────────────────────┘
                     │ 读取
┌────────────────────▼────────────────────────────┐
│                  渲染层                          │
│   js/                                            │
│   ├── main.js           （主入口，路由调度）      │
│   ├── render-systems.js  （渲染系统卡片/详情）     │
│   ├── render-features.js （渲染功能模块）          │
│   ├── render-changelogs.js（渲染版本日志）         │
│   └── components.js      （通用组件：导航/页脚）   │
└─────────────────────────────────────────────────┘
```

### 3.2 渲染流程

```
页面加载
  → main.js 初始化
  → 读取 site.json 获取站点配置（标题、导航等）
  → 根据 URL hash 判断当前页面（#/home, #/system/s01, #/changelog）
  → fetch 对应数据 JSON
  → 调用对应渲染函数生成 DOM
  → 绑定交互事件
```

**路由方案**：采用 Hash 路由（`#/system/s01`），GitHub Pages 不需要额外配置 404 重写，零成本支持多页面。

---

## 4. 页面结构设计

### 4.1 站点地图

```
首页 (#/)
├── Hero 区域（一句话定位 + CTA 按钮）
├── 系统矩阵概览（2~N 个系统卡片）
├── 核心优势（3~4 个亮点）
├── 最新动态（版本日志摘要）
└── 行动号召（了解更多 / 联系）

系统详情页 (#/system/{系统ID})
├── 系统头部（名称、定位、技术栈标签）
├── 核心功能模块（从 features.json 读取，图标+描述）
├── 技术架构亮点
├── 应用场景
├── 版本历程（时间轴展示）
└── 下载/文档/反馈入口

功能展示页 (#/features)
├── 全部功能模块总览
├── 按系统筛选
└── 功能详情卡片

版本更新页 (#/changelog)
├── 时间轴展示所有版本
├── 按系统筛选
├── 版本详情（新增/优化/修复分类）
└── 订阅更新（邮件列表，可选）

关于页 (#/about)
├── 开发者简介
├── 技术栈偏好
├── 联系方式
└── 开源计划
```

### 4.2 公共组件

| 组件 | 说明 |
|------|------|
| 顶部导航栏 | 固定顶部，响应式折叠为汉堡菜单 |
| 页脚 | 版权信息、友链、社交链接 |
| 系统卡片 | 展示系统缩略信息，点击进入详情 |
| 功能卡片 | 图标 + 标题 + 描述 |
| 时间轴 | 版本日志展示 |
| 模态框 | 截图预览、详情弹窗 |
| 回到顶部按钮 | 滚动时显示 |

---

## 5. 数据结构设计（核心 Schema）

> **这是整个网站的核心**。只要设计好 Schema，后续扩展就是加数据。

### 5.1 site.json - 站点全局配置

```json
{
  "siteName": "橙子的产品工坊",
  "siteSubtitle": "用代码解决实际教学与办公问题",
  "author": "橙子",
  "navItems": [
    { "label": "首页", "href": "#/" },
    { "label": "产品矩阵", "href": "#/products" },
    { "label": "功能一览", "href": "#/features" },
    { "label": "更新日志", "href": "#/changelog" },
    { "label": "关于", "href": "#/about" }
  ],
  "socialLinks": {
    "GitHub": "https://github.com/你的用户名",
    "Email": "mailto:你的邮箱@example.com"
  },
  "footerText": "© 2026 橙子的产品工坊. 保留所有权利."
}
```

> **键名说明**：所有 JSON 键名使用英文（避免编码兼容问题），值使用中文。编辑时只需修改值，键名保持不变。

### 5.2 systems.json - 系统列表（最核心）

```json
{
  "systems": [
    {
      "id": "s01",
      "name": "教师智能办公平台",
      "subtitle": "AI 赋能中小学教师全流程教学办公",
      "tagline": "基于私有知识库与大模型的本地智能备课与教学管理系统",
      "version": "v1.1.1",
      "status": "活跃开发中",
      "techStack": ["Python FastAPI", "原生HTML/JS", "Tailwind CSS", "SQLite", "Chroma", "DeepSeek API"],
      "tags": ["AI备课", "作业批改", "学生管理", "本地部署", "数据隐私"],
      "icon": "fa-chalkboard-user",
      "themeColor": "#3B82F6",
      "description": "面向中小学教师的本地智能备课与教学管理工具...",
      "keyMetrics": [
        { "label": "功能模块", "value": "10+" },
        { "label": "迭代版本", "value": "11" },
        { "label": "AI模型", "value": "DeepSeek" }
      ],
      "useCases": [
        "日常智能备课，一键生成教案/学案/习题/课件",
        "AI 视觉批改学生作业，归档成长档案",
        "班级与学生全生命周期管理",
        "生成交互式教学动画，课堂演示"
      ],
      "downloadUrl": "#",
      "docUrl": "#",
      "heroImage": "screenshots/s01-hero.png"
    },
    {
      "id": "s02",
      "name": "智能办公协作平台",
      "subtitle": "面向管理人员的文件收集与团队协作中枢",
      "tagline": "面向管理人员的轻量级文件收集、任务协作与智能分析平台",
      "version": "v1.1.1",
      "status": "活跃开发中",
      "techStack": ["Flask", "Vue 3", "Element Plus", "SQLAlchemy", "DeepSeek API"],
      "tags": ["文件收集", "任务协作", "报表分析", "智能搜索", "预约管理"],
      "icon": "fa-users-gear",
      "themeColor": "#10B981",
      "description": "面向学校行政与团队协作的轻量级办公平台...",
      "keyMetrics": [
        { "label": "功能模块", "value": "8+" },
        { "label": "迭代版本", "value": "11" },
        { "label": "AI分析", "value": "DeepSeek" }
      ],
      "useCases": [
        "批量文件收集（作业/材料/表格），进度实时追踪",
        "任务协作讨论，AI 自动总结讨论要点",
        "报表数据 AI 智能分析，自动生成洞察报告",
        "会议室/设备预约管理，冲突自动检测"
      ],
      "downloadUrl": "#",
      "docUrl": "#",
      "heroImage": "screenshots/s02-hero.png"
    }
  ]
}
```

### 5.3 features.json - 功能模块定义

```json
{
  "features": [
    {
      "id": "f001",
      "systemId": "s01",
      "systemName": "教师智能办公平台",
      "group": "AI 智能备课",
      "name": "智能备课生成",
      "description": "基于私有知识库与 DeepSeek 大模型，一键生成包含教学目标、重难点、教学过程、板书、作业、反思的完整备课方案",
      "highlights": [
        "流式生成，实时显示 AI 思考过程",
        "RAG 检索，优先引用教师上传的课标/课本/教案",
        "6 层稳定性保障，长思考模型兼容",
        "对话式修改，未修改内容自动保留"
      ],
      "icon": "fa-wand-magic-sparkles",
      "isHighlight": true,
      "sort": 1
    },
    {
      "id": "f002",
      "systemId": "s01",
      "systemName": "教师智能办公平台",
      "group": "AI 智能备课",
      "name": "一课多产",
      "description": "一次备课可同时生成教案、学案、习题、PPT 课件",
      "highlights": ["教案自动配套学案", "多难度习题生成", "PPT 课件一键导出"],
      "icon": "fa-layer-group",
      "isHighlight": false,
      "sort": 2
    },
    {
      "id": "f011",
      "systemId": "s02",
      "systemName": "智能办公协作平台",
      "group": "文件收集",
      "name": "智能文件收集任务",
      "description": "创建文件收集任务，参与者上传文件，管理员实时查看进度、批量下载、退回文件",
      "highlights": [
        "多任务并行，进度实时统计",
        "批量打包下载",
        "文件退回与重新上传",
        "软删除机制，被退回用户仍可下载历史文件"
      ],
      "icon": "fa-folder-tree",
      "isHighlight": true,
      "sort": 1
    }
  ]
}
```

### 5.4 changelogs.json - 版本更新日志

```json
{
  "changelogs": [
    {
      "id": "c001",
      "systemId": "s01",
      "systemName": "教师智能办公平台",
      "version": "v1.1.1",
      "date": "2026-08-23",
      "type": "功能更新",
      "added": [
        "资料中心支持图片上传，AI 自动识别图片内容建立索引",
        "多文件批量上传，自动整合为同一条课题记录",
        "每个文件支持独立的单文件重建索引"
      ],
      "improved": [
        "AI 生成系统 6 层稳定性保障，不再出现 JSON 解析失败",
        "对话修改内容防丢失，修改一处不影响其他内容",
        "Markdown 实时双通道预览"
      ],
      "fixed": []
    },
    {
      "id": "c005",
      "systemId": "s02",
      "systemName": "智能办公协作平台",
      "version": "v1.1.1",
      "date": "2026-05-30",
      "type": "功能更新",
      "added": [
        "普通用户支持下载自己上传的所有文件（打包为 zip）",
        "收集任务关联协作讨论主题"
      ],
      "improved": [
        "文件退回机制由物理删除改为软删除",
        "统计口径统一过滤已退回记录"
      ],
      "fixed": []
    }
  ]
}
```

### 5.5 testimonials.json - 用户评价/案例（可选，预留扩展）

```json
{
  "testimonials": [
    {
      "id": "t001",
      "author": "张老师",
      "role": "某中学语文教师",
      "systemId": "s01",
      "content": "备课效率提升了至少 3 倍，AI 生成的教案贴合教材，对话修改非常方便。",
      "avatar": "",
      "rating": 5
    }
  ]
}
```

### 5.6 Schema 设计原则

1. **键名用英文，值用中文**：避免编码兼容问题，JSON 更稳健；编辑时只改值即可
2. **每条记录有唯一 id**：用于路由跳转和关联（如 `s01`、`f001`、`c001`）
3. **系统与功能/日志通过 `systemId` 关联**：解耦，增删系统不影响其他数据
4. **冗余存储 `systemName`**：功能和日志中同时存系统名，单文件可读，减少联表
5. **预留扩展字段**：如 `heroImage`、`tags`、`sort` 等
6. **可选字段不强制**：如 `downloadUrl` 可以为空字符串

---

## 6. 目录结构规划

```
个人网站/
├── index.html              # 入口页面（SPA 壳）
├── 404.html                # GitHub Pages 404 页（可选）
├── robots.txt              # 搜索引擎爬虫规则
├── sitemap.xml             # 站点地图（SEO）
│
├── data/                   # 数据层（核心！改内容只动这里）
│   ├── site.json           # 站点全局配置
│   ├── systems.json        # 系统列表
│   ├── features.json       # 功能模块
│   ├── changelogs.json     # 版本日志
│   └── testimonials.json   # 用户评价（可选）
│
├── css/                    # 样式层
│   └── style.css           # 自定义补充样式（Tailwind 为主）
│
├── js/                     # 渲染层
│   ├── main.js             # 主入口 + 路由
│   ├── components.js       # 通用组件（导航/页脚/卡片）
│   ├── render-systems.js   # 系统页渲染
│   ├── render-features.js  # 功能页渲染
│   ├── render-changelogs.js# 日志页渲染
│   └── utils.js            # 工具函数（fetch/format 等）
│
├── assets/                 # 静态资源
│   ├── images/
│   │   ├── logo.png        # 站点 Logo
│   │   ├── favicon.ico     # 网站图标
│   │   └── screenshots/    # 系统截图（按系统ID分目录）
│   │       ├── s01/
│   │       └── s02/
│   └── files/              # 可下载的文档/安装包
│       └── README.md
│
└── .github/                # GitHub 配置
    └── workflows/
        └── deploy.yml      # 可选：自动部署（其实直接推送即可）
```

---

## 7. 扩展性设计（重点）

> 你的核心诉求是「不要写死，后面还能增加内容和系统」。以下是各类扩展的具体操作方式。

### 7.1 如何添加第三套系统（S03）

**只需 3 步，零代码修改**：

1. **在 `data/systems.json` 的 `systems` 数组追加一条记录**：
   ```json
   {
     "id": "s03",
     "name": "你的第三套系统名称",
     "subtitle": "...",
     "tagline": "...",
     ...
   }
   ```

2. **在 `data/features.json` 中添加该系统的功能模块**（`systemId` 填 `s03`）

3. **在 `data/changelogs.json` 中添加该系统的版本日志**（`systemId` 填 `s03`）

4. **（可选）把截图放到 `assets/images/screenshots/s03/` 目录**

5. `git push` 后网站自动更新，首页系统矩阵自动多出一张卡片

**前端代码完全不需要改**——渲染函数是遍历 `systems` 动态生成的。

### 7.2 如何给现有系统添加新功能

1. 在 `data/features.json` 的 `features` 数组追加一条记录
2. 设置 `systemId` 为对应系统，`sort` 控制展示顺序
3. 推送即可，功能页和系统详情页自动更新

### 7.3 如何发布新版本日志

1. 在 `data/changelogs.json` 的 `changelogs` 数组追加一条记录
2. 填写版本号、日期、新增(added)/优化(improved)/修复(fixed)内容
3. 推送即可，版本页自动按时间轴倒序展示，首页"最新动态"自动更新

### 7.4 如何添加新页面/新导航项

1. 在 `data/site.json` 的 `navItems` 数组追加一项
2. 在 `index.html` 中添加对应的页面容器 `<section id="page-xxx">`
3. 在 `js/main.js` 的路由表中添加一行映射
4. 编写对应的渲染函数 `js/render-xxx.js`
5. 推送即可

### 7.5 如何更换主题色/品牌色

- 全站主题色通过 CSS 变量定义，修改 `css/style.css` 顶部的 `:root` 变量即可
- 每套系统的卡片主题色在 `systems.json` 的 `themeColor` 字段单独配置

### 7.6 如何接入评论/反馈功能（未来）

预留扩展点：
- 方案A：接入 Giscus（基于 GitHub Discussions，免费）
- 方案B：接入 Utterances（基于 GitHub Issues，免费）
- 只需在页面底部加一段 script，不动数据结构

---

## 8. UI/UX 设计规范

### 8.1 设计风格

- **整体风格**：现代简洁 + Fluent Design 质感
- **布局**：大量留白，卡片式设计，圆角 + 柔和阴影
- **动效**：滚动淡入、悬停微交互、页面切换过渡
- **色彩**：
  - 主色：深蓝 `#3B82F6`（专业感）
  - 辅助色：按系统区分（s01 蓝、s02 绿，未来系统可自定义）
  - 中性色：灰阶为主，内容优先

### 8.2 响应式断点

| 断点 | 宽度 | 布局 |
|------|------|------|
| 手机 | < 640px | 单列，汉堡菜单 |
| 平板 | 640px ~ 1024px | 2 列卡片 |
| 桌面 | > 1024px | 3~4 列卡片，完整导航 |

### 8.3 字体规范

- 中文：思源黑体 / 苹方 / 微软雅黑
- 英文/数字：Inter / system-ui
- 标题：加粗，层级分明
- 正文：行高 1.7，字号 16px

### 8.4 图标规范

- 统一使用 Font Awesome 6（CDN）
- 每套系统指定一个主图标
- 功能模块统一用线性图标风格

---

## 9. GitHub Pages 部署方案

### 9.1 仓库准备

1. 在 GitHub 创建仓库，建议命名：`username.github.io`（个人主页）或 `product-site`（项目页）
2. 本地初始化 git，推送代码到仓库

### 9.2 启用 GitHub Pages

1. 仓库 → Settings → Pages
2. Source 选择 `Deploy from a branch`
3. Branch 选择 `main` / `master`，目录选择 `/ (root)`
4. 保存后等待 1~2 分钟，访问 `https://用户名.github.io/仓库名/` 即可看到网站

### 9.3 自定义域名（可选）

- 在域名服务商添加 CNAME 记录指向 `用户名.github.io`
- 在仓库根目录添加 `CNAME` 文件，内容为你的域名
- GitHub Pages 自动配置 HTTPS

### 9.4 更新流程

```bash
# 改了数据文件或代码后
git add .
git commit -m "更新：新增 s03 系统 / 发布 v1.2.0 日志"
git push
# GitHub Pages 自动部署，1 分钟后生效
```

### 9.5 注意事项

- Hash 路由（`#/system/s01`）不需要服务器配置，GitHub Pages 完美支持
- JSON 文件通过 `fetch()` 加载，GitHub Pages 支持正确的 MIME 类型
- 所有资源使用相对路径，确保项目子路径部署也能正常工作

---

## 10. 开发计划与里程碑

| 阶段 | 内容 | 预计耗时 | 产出 |
|------|------|----------|------|
| **M1 基础框架** | index.html 骨架 + 导航栏 + 页脚 + 路由 + 数据加载 | 0.5 天 | 可访问的空壳站点 |
| **M2 数据填充** | 编写 systems/features/changelogs JSON，整理现有系统资料 | 0.5 天 | 数据层完成 |
| **M3 页面渲染** | 首页 + 系统详情页 + 功能页 + 日志页渲染逻辑 | 1 天 | 核心页面可用 |
| **M4 样式打磨** | Tailwind 样式 + 响应式适配 + 动效 + 截图占位 | 0.5 天 | 视觉完成 |
| **M5 部署上线** | GitHub 仓库创建 + Pages 配置 + 内容校对 | 0.5 天 | 正式上线 |
| **M6 持续维护** | 添加真实截图、下载链接、用户评价等 | 按需 | 持续迭代 |

---

## 11. 后续扩展方向

| 方向 | 说明 | 优先级 |
|------|------|--------|
| 第三套系统 | 按 S01/S02 的 Schema 追加即可 | 待定 |
| 在线演示/Demo | 嵌入演示视频或在线试用入口 | 中 |
| 下载统计 | 接入 GitHub Releases 或第三方统计 | 低 |
| 多语言支持 | JSON 增加 `英文` 字段，前端切换 | 低 |
| 博客/文章 | 新增 `posts.json` + 博客页，分享技术文章 | 中 |
| 在线客服 | 接入 Giscus 或 Telegram Bot | 低 |
| PWA 支持 | 添加 manifest.json，可"安装"到桌面 | 低 |
| 暗色模式 | 跟随系统或手动切换 | 中 |

---

## 附录：关键决策记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 架构模式 | 数据驱动 + 纯静态 | 满足「不写死」核心诉求，维护成本最低 |
| 路由方案 | Hash 路由 | GitHub Pages 零配置，直接支持 |
| CSS 方案 | Tailwind CDN + 自定义补充 | 快速开发，无需构建 |
| 数据源 | JSON 文件 | 人类可读，易于编辑，无需数据库 |
| 部署平台 | GitHub Pages | 免费、稳定、与代码仓库一体 |
| 扩展方式 | Schema 追加数据 | 新增系统零代码修改 |

---

> **下一步**：确认本开发文档后，即可进入编码阶段，按 M1→M5 顺序实现。
> 如需调整方案（例如改用 VitePress 或增加暗色模式），请在编码前提出。
